apiVersion: 1

providers:
  - name: NatureProtectorDashboards
    folder: NatureProtector
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards