# System Capacity Model

Capacity is reported as local observed processed events per second. One-replica baseline is derived from the live autoscaling campaign and must not be generalized to production.

Autoscaling target backlog is generated by `scripts/autoscaling/analyze-capacity.py` from measured one-replica throughput and configured acceptable queue delay.
